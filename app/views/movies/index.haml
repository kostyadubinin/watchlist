-# TODO: Decorate and localize.
.ui.four.column.stackable.doubling.grid
  - @movies.each do |movie|
    - movie_presenter = present(movie, with: MoviePresenter)
    .column
      .ui.fluid.card
        .image= image_tag(movie_presenter.backdrop_url)
        .content
          .header= movie["title"]
          .meta
            %span= movie["release_date"]
        .extra.content
          %i.fa.fa-star
          = movie["vote_average"]
          .right.floated
            %i.fa.fa-users
            = movie["vote_count"]
